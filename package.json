{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "deploy": "composer install && npm install && npm run build && php artisan storage:link && php artisan db:seed --class=EventSeeder && php artisan db:seed --class=TicketSeeder ",
        "deploymigration": "php artisan migrate:fresh --seed && composer install && npm install && npm run build"
    },
    "devDependencies": {
        "@tailwindcss/line-clamp": "^0.4.4",
        "@tailwindcss/typography": "^0.5.15",
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-echo": "^1.17.1",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.49",
        "pusher-js": "^8.4.0-rc2",
        "tailwindcss": "^3.4.15",
        "vite": "^5.0"
    }
}
